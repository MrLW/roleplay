package com.sim.server.game.utils;

public class ActionMapUtil {

	/*private static Map<Short, Action> map = new HashMap<Short, Action>();  
	  
    public static Object invote(Short key, Object... args) throws Exception {  
        Action action = map.get(key);  
        if (action != null) {  
            Method method = action.getMethod();  
            try {  
                return method.invoke(action.getObject(), args);  
            } catch (Exception e) {  
                throw e;  
            }  
        }  
        return null;  
    }  
    
    public static void put(Short key, Action action) {  
        map.put(key, action);  
    }  */
}
